/**
 * Centred column for inner pages + footer inner grids.
 * Horizontal padding comes from `.excelia-page-container` in `globals.css` so gutters always apply.
 */
export const PAGE_CONTAINER_CLASS =
  "excelia-page-container mx-auto w-full min-w-0 max-w-6xl";
